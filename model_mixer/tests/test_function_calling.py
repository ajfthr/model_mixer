# This file will contain unit tests that check the code's ability to pass defined
# functions to the providers and handle the results.

# Ideally, we should take function definitions as an OpenAPI spec and handle function definition formatting ourselves.
